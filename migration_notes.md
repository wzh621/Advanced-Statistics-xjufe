# Migration notes

本文件记录迁移过程中发现的原稿疑点、必要的兼容性处理及其处置。原始材料不做改写。

## 全项目约定

1. 中文版与英文版必须保持相同的概念顺序、公式、例题、图形和 R 分析方法；英文版是教学翻译，不是缩写版。
2. 每章完成前必须对照相应章节主文件及全部 `tex/chap-*/*.tex` 片段。源文件中有内容但讲义没有落点时，该章不得标记为完成。
3. 允许增加过渡说明、直观解释和必要练习，但新增内容不得替代或冲淡原课件内容；扩展内容应能与源内容明确区分。
4. 所有生成图形通过 `showtext::showtext_auto()` 使用跨平台字体。macOS 本机构建自动选用现有 CJK 字体；Linux/GitHub 构建推荐安装 `Noto Sans CJK SC`。
5. 网站的二级节使用独立教学画布，提供接近 PPT 的视觉节奏；正文仍保留连续阅读、全文检索和章节交叉引用能力。

## 第 1 章

1. `1-2_PT.tex` 的实直线 $\sigma$-代数示例写作 `A_i=[1/i]`，但同时声称其并集为 $(0,1]`。结合上一节同一示例，本讲义采用 `A_i=[1/i,1]` 的含义描述闭包问题，没有复写原稿中的不一致表达。
2. `1-2_PT.tex` 的可数可加性公式缺少 `\bigcup_{i=1}^{\infty}` 的完整下标结构；迁移时只修正排版。
3. `1-3_CP.tex` 的四张 A 示例把 $i=1$ 时的条件概率写成 `0.0005`。按原稿公式计算应约为 `0.000048`；正文采用计算结果并明确说明差异。
4. `1-3_CP.tex` 的骰子示例包含 `1//36`，迁移时按其等式含义排版为 `1/36`。
5. `1-3_CP.tex` 的无限二进制序列示例出现 `2_i`，结合后续公式应为 $2^i$；正文仅保留该例要表达的相互独立结论。
6. `1-4_RV.tex` 将满足可测性条件的函数称为 “real variable”；正文按本节标题及标准术语写作“实值随机变量”。
7. `1-5_DF.tex` 的离散 cdf 表最后一区间写作 `[3,8)`，但表中 $X$ 的最大值为 3；正文改为 `[3,\infty)`。
8. `1-5_DF.tex` 的右连续条件漏写极限中的 $F(x)$；正文补全为 $\lim_{x\downarrow x_0}F(x)=F(x_0)$。
9. `1-3_CP.tex` 的“三名死囚”只有题目开头，“生日问题”只有主题名称。初次迁移均保留缺失说明；2026-09-01 对照教材 Example 1.3.4 后已恢复“三名死囚”完整题干与解答。“生日问题”仍因资料不足未补充。
10. `pic/chap-1.R` 原来通过 `png()` 和 `dev.off()` 写出图形。迁移后交由 knitr 管理图形设备，函数、区间、步长和两条曲线的含义不变。
11. 三次抛硬币的 R 示例来自 `statistical_inference_code/week_1/random variables and univariate distributions.rmd`。迁移时保留相同的八个样本点和正面次数，以基础 R 汇总概率，避免依赖仅用于绘图的旧式 ggplot2 `..density..` 语法。

## 第 0 章

1. 用户给出的路径 `课程课件B/tex/chap-0/` 在当前项目中不存在；实际非空源文件为 `课程课件B/chap-0.tex`，且不包含正文的 `input` 或 `include` 依赖。
2. 原课件的教师信息、五篇代表作、课程基本信息、课程介绍、教材、参考书、教学任务、教学目标、课程内容、授课方式和考核比例均已进入中英文讲义。Beamer 主题、水印、目录页和逐条显示命令不作为教材正文迁移。
3. 原课件个人信息二维码复制到 `images/ch00/wang-info.png`；原图保持不变。

## 第 2 章

1. `2-1_DF.tex` 将分布函数写成事件 `Y in y`，并在积分集合中混用括号；正文按定义改为 `Y <= y` 与集合 `{x: g(x) <= y}`。
2. 正弦平方例题的原推导缺少四个交点的定义且末行不闭合。正文保留原图并给出完整的分布函数与密度。
3. 标准正态平方例题误把求导结果中的标准正态密度写成分布函数，且最终密度式错误。正文更正为卡方自由度 1 的密度。
4. `2-2_EV.tex` 的期望线性性质漏写第二项的期望符号；平方损失最小化式的括号也不完整。正文分别补全。
5. Cauchy 例题的支撑区间排印不完整；正文补为实数轴，并说明期望不存在的原因是绝对一阶矩发散。
6. `2-3_MM.tex` 的对数正态密度缺少明确的乘号与指数函数结构；正文按该经典矩不确定例补全标准密度。
7. 原稿关于矩母函数导数的条件写作“对某个正数 t 存在”。为支持零点处各阶求导，正文采用在零点某个邻域内存在的标准条件。
8. Poisson MGF 的原求和第二步从 1 开始，却直接使用完整指数级数；正文统一从 0 开始。
9. 二项到 Poisson 的例题把两个分布的点概率写成相等。正文按原 MGF 极限证明改为渐近近似与依分布收敛。
10. 原课件对 Leibniz 法则仅列“可微”条件，未完整陈述交换求导与积分所需的正则性条件。正文保留公式并明确需连续性和可积控制。
11. 第 2 章源文件没有 R 代码。讲义新增正弦平方变换模拟与二项 MGF 数值求导两个教学扩展，统计方法和原始数学内容未被替换。

## 第 3 章

1. `3-1_DD.tex` 把 Binomial MGF 写成 Bernoulli MGF；正文补上 $n$ 次方。
2. 原稿对 $\{0,\ldots,n\}$ 上离散均匀分布的 MGF 漏掉分母 $e^t-1$，方差写成 $(n^2-1)/12$；正文分别更正为 $\{e^{(n+1)t}-1\}/\{(n+1)(e^t-1)\}$ 与 $n(n+2)/12$。
3. `3-1_DD.tex` 的 Negative Binomial 概率函数把变量定义为第 $r$ 次成功前的失败数，但 MGF 和均值使用了另一种参数化且仍不一致。正文以原概率函数为准，采用 $M(t)=\{p/(1-(1-p)e^t)\}^r$、$E(X)=r(1-p)/p$，并说明总试验次数参数化需平移 $r$。
4. `3-2_CD.tex` 将 Beta、Laplace、Logistic 和 Uniform 的 MGF 一概写成不存在。正文区分“没有初等闭式”和“在零点邻域不存在”，并给出实际存在的 MGF 与定义域。
5. 原稿 Gamma 采用形状—尺度密度，却把 MGF 写成 $(1-t/\theta)^{-k}$ 且给出 $t<\theta$；正文更正为 $(1-\theta t)^{-k}$、$t<1/\theta$。
6. 原稿 $F(d_1,d_2)$ 的密度与方差混用了自由度。正文采用标准密度，并将方差更正为 $2d_2^2(d_1+d_2-2)/\{d_1(d_2-2)^2(d_2-4)\}$。
7. 原稿声称 Weibull 均值只在 $k>1$、方差只在 $k>2$ 时存在。正文说明任意 $k>0$ 时所有正阶矩均存在，并按 $k$ 的范围说明 MGF 的存在性。
8. `3-3_EF.tex` 的正态例同时使用 $x$ 与 $y$；正文统一为 $y$。Gamma 指数族重参数化把 $c(y,\phi)$ 错放进除以 $\phi$ 的括号，正文重新推导并给出可还原原密度的表达。
9. Bernoulli 指数族例在设定 $n=1$ 后仍保留 $b(\theta)=n\log(1+e^\theta)$；正文明确写为 $b(\theta)=\log(1+e^\theta)$。
10. 指数族性质的原证明在二阶导数积分中多乘一个密度，并在方差推导末行出现符号错误。正文在支撑集与交换求导的正则条件下重写连续证明，并补充离散求和版本。
11. `3-4_DU.tex` 的定理 3.5.1 漏写位置常数 $\mu$；正文补全 $\mu\in\mathbb R$ 并给出密度归一化验证。
12. 主文件引用的 `pic/fig_distr.png` 已复制到中英文章节资源目录并保留原图说明。第 3 章源文件没有 R 代码；讲义新增离散 PMF 和重尾密度比较两个基础 R 教学图，不替换原有公式、例题或关系图。
13. 本轮将指数族性质 1、定理 3.5.1 与定理 3.5.6 的证明，以及正态、Poisson 和指数位置族例的计算过程改为默认折叠。依据分别为 `3-3_EF.tex`、`3-4_DU.tex` 与 Casella--Berger 第 3.4--3.5 节；中英文内容保持同范围。
14. 补回教材定理 3.5.7（位置—尺度变换下的均值和方差）及其证明，来源为 Casella--Berger 第 3.5 节（印刷页 120--121）。该条承接原课件定理 3.5.6，不改变既有自动编号或交叉引用。

## 第 4 章

1. `4-2_CD.tex` 的条件期望与条件方差对条件变量积分时误写为 `dx`；正文按积分变量改为 `dy`。
2. `4-4_HM.tex` 的 Poisson 稀疏化推导在指数级数处漏掉正号；正文采用 `exp((1-p)lambda)`，从而得到正确的 Poisson(`lambda p`) 边缘分布。
3. 同文件的 Poisson--指数混合例把正参数的积分下限写成负无穷；正文改为 0，并明确指数分布采用尺度参数 `beta`。
4. Beta--Bernoulli 例把条件分布写作缺少试验次数的 Binomial(`P_i`)，末行又混用 `h` 与 `n`；正文写为 Bernoulli(`P_i`) 并统一样本容量符号。
5. 全方差公式证明含重复首行及 `Var(E[X|]Y)` 排印错误；正文重排推导，不改变证明思路。
6. 多元正态密度混用随机向量与取值符号，二维密度的指数项漏掉 `1/(1-rho^2)`；正文统一符号并补全系数。
7. Lyapunov 不等式右端误写为 `(E|X|)^(1/p)`；正文更正为 `(E|X|^p)^(1/p)`。Young 不等式同时补充 `p,q>1`。
8. 原课件的 `MD.png` 与 `CD.png` 带有截图时的交互提示浮层，仍按源图保留；另增加可执行的二维正态等高线 R 图帮助阅读。
9. 第 4 章源文件没有 R 代码。新增的二维正态图只用于教学可视化，不替换原有方法或推导。
10. 本轮把例 4.1.11、例 4.1.12、例 4.2.4、Poisson 稀释、Poisson--指数混合、正态和的推导，以及 Young、Hölder、Minkowski、Jensen 和全方差公式的证明改为默认折叠。依据为 `4-1_JM.tex`、`4-2_CD.tex`、`4-4_HM.tex`、`4-6_IN.tex` 与 Casella--Berger 第 4.1--4.7 节。
11. 原课件例 4.3.3 只停在 $(U,V)$ 的联合密度。本轮依据 Casella--Berger 第 4.3 节例 4.3.3（印刷页 158--159）补完对 $V$ 的积分与 Beta 积分换元，恢复结论 $XY\sim\operatorname{Beta}(\alpha,\beta+\gamma)$；中英文推导一致。
12. 联合函数期望、可分解判别独立、独立变量函数乘积期望和全期望公式的证明依据教材对应定理补全。对教材仅把等号必要条件留作习题的 Jensen 结论，正文只保留教材已证明的不等式与可直接陈述的等号说明，没有自行补造教材省略的证明。
13. 按课堂展示需求保留图 4.1 原课件图片，并新增 Plotly 交互复现。交互图明确采用标准独立二元正态模型 $f(x,y)=\phi(x)\phi(y)$，曲面表示联合密度、边界曲线表示两个边缘密度；这属于对原图表现方式的复现，不替换原图或改变课程结论。Plotly.js 4.0.0 从官方 CDN 本地化到 `assets/js/`，构建时复制到中英文输出目录，避免 GitHub Pages 依赖外部 CDN。

## 第 5 章

1. `chapter-5.md` 在“可直接利用 iid 性质计算”之后突然截断。正文保留此前全部补充叙述，并补写 `prod_i P(X_i>2)=exp(-2n/beta)`；原文件保持不变。
2. `5-2_SR.tex` 的样本均值 MGF 证明引入未定义的 `Y`；正文直接用独立性分解指数乘积。卷积证明末尾混用 `u` 与 `z`，正文统一变量。
3. 同文件把正态分布写作小写 `n`，并有多处 Definition 拼写错误；正文仅作术语和排印标准化。
4. `5-4_Co.tex` 的几乎处处收敛定义写成 `P(lim |X_n-X|<epsilon)=1`，不能准确表达逐点极限；正文改为 `P(lim X_n=X)=1`，并保留零概率例外集的解释。
5. 原稿的强大数定律使用有限方差作为充分条件；正文保持这一较强但有效的条件，不扩张为必要条件。
6. 样本方差相合性的 Chebyshev 论证仅表明 `Var(S_n^2)->0` 是充分条件；正文明确其逻辑地位，避免在缺少高阶矩条件时过度断言。
7. 原稿先陈述矩母函数在零点邻域存在的 CLT，再陈述有限方差版本；中英文正文均保留两者关系，并注明后者可用特征函数证明。
8. Slutsky 例中的极限下标排印错误和小写正态记号已标准化。
9. 多元 Taylor 近似中 `g(theta)` 与偏导下标混用标量、向量；正文统一为粗体参数向量并保留协方差项。
10. Delta 方法原证明只说余项趋于 0，不足以支持乘以 `sqrt(n)`；正文采用标准的 `o_p(n^(-1/2))` 余项表述。
11. 倒数样本均值的插补方差原式漏掉 `/n`；正文更正为 `S^2/(n bar(X)^4)`，相应学生化极限式保持一致。
12. 第 5 章源文件没有 R 代码。中英文讲义新增指数寿命模拟、均匀顺序统计量和均匀最大值极限三个基础 R 图；所有 chunk 标签唯一，统计方法未被替换。
13. 本轮逐项核对 Casella--Berger 第 5.1--5.5 节（印刷页 207--244；PDF 页 235--272）与 `legacy/latex/课程课件B/tex/chap-5/`。新增内容均采用教材原编号，详细证明与解答放入 HTML5 `details` 折叠区，中英文保持同范围。
14. 有限总体近似例 5.1.3、离差平方和定理 5.2.4、iid 和引理 5.2.5、定理 5.2.6 的完整证明，以及正态与 Gamma 样本均值计算，依据教材印刷页 210--215；这同时修正了旧 Bookdown 把定理 5.2.6 误限于正态总体的问题。正态性仅用于识别样本均值的完整分布。
15. Cauchy 和例 5.2.10 依据教材印刷页 216--217。教材把卷积积分的部分分式与反导细节留作练习 5.7，正文忠实保留这一边界，只展开卷积设置、教材给出的密度结果及 iid 样本均值结论，不自行补造被省略的积分步骤。
16. 补回正态样本定理 5.3.1 及其独立性、卡方递推证明，依据教材印刷页 218--220；该定理为原课件直接定义 Student $t$ 与 Snedecor $F$ 提供必要的抽样分布基础。
17. 顺序统计量定理 5.4.3、5.4.4 的计数证明与均匀例 5.4.5，依据 `5-3_OS.tex` 及教材印刷页 227--230；连续型证明用教材的“小区间 + 多项式计数”论证展开，未改变原公式。
18. 弱大数定律的 Chebyshev 证明、样本方差相合性的条件边界、均匀最大值例 5.5.11、MGF 版 CLT 证明、学生化例 5.5.18 与 Taylor/Delta 推导，依据 `5-4_Co.tex` 及教材印刷页 232--244。教材未给出的有限方差 CLT 特征函数证明和 Slutsky 定理证明仍不补写。
19. 补回教材定理 5.5.26（二阶 Delta 方法），用于解释 $g'(\theta)=0$ 时一阶 Delta 方法为何退化。例 5.5.25 的插补方差继续采用已订正的 $S^2/(n\bar X^4)$；原课件漏写的 `/n` 不恢复到学生页面。

## 第 6 章

1. `6-2_SP.tex` 的正态充分性例一处写“sigma 未知”，但推导固定 sigma 并只研究 mu；正文按后续重复例的设定明确为 sigma 已知。样本均值密度的归一化常数指数也由误写的 `-n/2` 更正为 `-1/2` 的含义。
2. 离散均匀分布例原来的 `h(x)` 含参数 theta，不能作为因子分解定理中的无参数因子；正文把样本取值为正整数的指示函数放入 `h`，把 `X_(n)<=theta` 放入 `g`。
3. 正态双参数例的 `g` 含双重负号，并把均值平方项系数误写为 `n-1`；正文按平方和分解更正为 `[(n-1)s^2+n(bar(x)-mu)^2]/(2sigma^2)`。
4. `U(theta,theta+1)` 的联合密度支撑误写为 `max x_i < theta < min x_i`；正文更正为 `x_(n)-1 < theta < x_(1)`，并据此保留最小充分性论证。
5. `U(0,theta)` 最大值完备性证明在乘积求导中直接把第二项写成 0；正文先利用期望恒为 0 得到积分恒为 0，再求导，避免遗漏理由。
6. Basu 定理不要求另加“最小”条件；正文采用标准的“完备充分统计量与辅助统计量独立”。原稿定理 6.2.28 漏写“充分”，正文更正为“任何完备充分统计量也是最小充分统计量”。
7. 似然原则原稿只写推断应基于 `C*L`，正文补全为两个似然函数相差与参数无关的正常数时表达相同证据。二项等变例的多余右括号及重复句也作排印整理。
8. 第 6 章源文件没有 R 代码。新增均匀位置族极差模拟，用来验证辅助统计量的分布不依赖位置参数，不替换原有理论推导。
9. 本轮依据 Casella--Berger 第 6.2 节（印刷页 271--289；PDF 页 299--317）核对数据约简章节。因子分解定理 6.2.6 的双向证明、最小充分判别定理 6.2.13 的等价类论证和 Basu 定理 6.2.24 的完整证明，均恢复到中英文折叠区；学生页面默认只显示定理条件与结论。
10. 例 6.2.3 与例 6.2.7 的题目、编号和计算依据教材对应例题及 `legacy/latex/课程课件B/tex/chap-6/6-2_SP.tex`。详细解答改为 HTML5 `details`，未改变既有统计方法和符号体系。
11. 教材对一般连续型因子分解定理采用测度论表述，而当前课程原稿只要求离散推导及连续类比。本轮完整展开离散双向证明，并明确连续情形以密度和积分替换质量与求和，没有自行加入超出课程层次的测度论证明。

## 第 7 章

1. 矩估计的二项例保留原公式，并补充 `bar(X)-v_n<=0` 时无内部可行解、整数参数 `k` 需施加约束，避免把形式解当作自动有效的估计值。
2. 最大似然定义中的样本列表把末项误写为 `x_k`；正文统一为 `x_n`，并补充边界与参数约束检查。Gamma 双参数例只说“数值求解”，正文给出 `beta=bar(y)/alpha` 与 digamma 方程。
3. `statistical_inference_code/week_3/example on likelihood.R` 直接计算 300 个指数密度的乘积，容易数值下溢，且加载了未使用的 ggplot2。迁移后保留指数尺度模型、随机样本与网格思想，改为基础 R 对数似然，并采用唯一 chunk 标签。
4. Beta--Binomial 例在未说明损失时称后验均值为 Bayes 估计；正文明确这是平方误差损失下的结论。
5. EM 原稿交替写“最大化似然的期望”与“最大化对数似然的期望”；正文统一为完整数据对数似然的条件期望 `Q`。缺失响应回归例明确 sigma 已知，并说明在固定设计和可忽略缺失下，缺失响应本身不增加 beta 的观测信息。
6. MSE 分解漏掉偏差平方；正文更正为 `Var(W)+Bias(W)^2`。正态方差 MLE 与无偏样本方差的 MSE 比较保持原方法。
7. `pic/501005.pdf` 为单页源图，转换成网页 PNG 后在中英文版保留，原 PDF 不变。
8. 非正则均匀模型中，得分对 lambda 求导、得分期望及所谓下界均混用符号且不满足正则条件；正文不复写无效下界，直接说明参数相关支撑导致微分与积分不可交换，并保留最大值无偏估计量及其方差。
9. 均匀最大值方差推导中一行漏写缩放系数的平方；正文给出最终正确结果 `theta^2/[n(n+2)]`。
10. 正态似然的指数分母误写为 sigma 而非 sigma 平方；正文更正。风险例声称最优于“所有估计量”，正文收窄为原计算实际证明的 `bS^2` 类。
11. 定理 7.3.20 原稿称 UMVUE 与“所有 theta 的无偏估计量”不相关；标准条件是与所有期望恒为 0 的无偏估计量不相关，正文据此更正。Lehmann--Scheffe 结论明确为完备充分统计量函数是其期望的唯一 UMVUE。
12. 二项 Rao--Blackwell 例的联合概率分子漏写概率符号；正文补全并保留最终组合数公式。正态--正态例的小写分布记号统一为 `N`。
13. 本轮依据 Casella--Berger 第 7.3 节（印刷页 335--347；PDF 页 363--375）补回定理 7.3.9 的得分协方差与 Cauchy--Schwarz 证明、定理 7.3.17 的全期望/全方差证明，以及定理 7.3.23 的 Rao--Blackwell 加完备性证明；中英文均置于默认折叠区。
14. 二项矩估计例 7.2.2、MLE 不变性定理 7.2.10 与 Beta--Binomial 例 7.2.14 依据教材第 7.2 节及 `legacy/latex/课程课件B/tex/chap-7/7-2_MF.tex` 补回中间推导；Poisson 信息下界例 7.3.12、参数相关支撑例 7.3.13 和二项 UMVUE 例 7.3.24 依据教材相应例题及 `7-3_ME.tex` 整理为“题目在外、详细解答折叠”的课堂展示结构。Poisson 例补回得分和信息的中间计算；均匀例继续强调正则条件失败，不套用无效下界。
15. 本轮没有补造教材省略的技术证明。MLE 非一一变换、EM 收敛的更一般条件以及决策论的一般存在性结论仍保持当前课程材料的说明层次。

## 第 8 章

1. 原稿多处使用“接受原假设为真”。正文改用“不拒绝原假设”，以避免把证据不足误解为证明原假设。
2. 正态未知方差 LRT 例在分段式中把边界写成 `hat(mu) <= mu`，并在受限方差中继续混用 `mu` 与 `mu_0`；正文统一为 `mu_0`，保留其等价单侧 t 检验形式。
3. 正态 Bayes 检验的后验均值原式包含无意义的 `tilde{sigma^2 mu}`；正文按正态--正态共轭结果排为 `(n tau^2 bar(x)+sigma^2 mu)/(n tau^2+sigma^2)`。
4. 正态均值 LRT 在方差已知为 1 时无需 CLT，标准化统计量具有精确正态分布；正文给出精确结论。
5. Neyman--Pearson 引理的必要性部分原稿有 `satisfes` 和零测集表述排印问题；正文保留充分性、必要性与充分统计量推论的数学含义，并统一术语。
6. 原稿双侧无偏检验的标题把备择写成 `theta != theta_1`，正文依据该例后续公式统一为 `theta != theta_0`。
7. `501007.pdf`、`501009.pdf` 转成网页 PNG，`umpt.jpg` 与 `pvalue.jpg` 保持源图内容。中英文版共享同一资源副本。
8. 第 8 章源文件没有 R 代码；新增二项功效函数基础 R 图，保留原课件的两种拒绝规则与功效公式。
9. 本轮逐项核对 Casella--Berger 第 8.1--8.3 节（印刷页 373--398；PDF 页 401--426）与 `legacy/latex/课程课件B/tex/chap-8/`。例 8.2.2 的正态 LRT 计算、定理 8.2.4 的因子分解证明、定理 8.3.12 的 Neyman--Pearson 积分不等式证明、定理 8.3.17 的 Karlin--Rubin 论证和定理 8.3.27 的有效 p 值证明，均恢复到中英文折叠区。
10. Neyman--Pearson 引理按教材先证明连续情形，并说明离散情形以求和替换积分；必要性只在两种简单假设下均为零概率的集合之外成立。Karlin--Rubin 证明使用教材给出的 MLR 单调功效与 Neyman--Pearson 推论，没有把复合假设结论扩大到不满足 MLR 的模型。
11. 教材的并交检验、接受抽样和第 8.4 节习题不在当前课件迁移范围内；本轮未自行把这些未迁移材料扩张进讲义，也未生成教材未提供的习题答案。

## 第 9 章

1. 区间估计定义原稿把随机区间写成 `[L(x),U(x)]`；正文区分随机区间 `[L(X),U(X)]` 与观测后的实现值。
2. 指数 LRT 反演中原稿先设 `a>b`，随后写 `int_a^b=1-alpha`，积分方向矛盾；正文按 `b<T/lambda<a` 写成概率约束，并保持端点等似然条件。
3. 位置--尺度枢轴量例把复数名词写成 `a pivots`；正文统一为 pivotal quantity，并说明同时含两个未知参数时必须交代目标参数和干扰参数处理。
4. Gamma 枢轴例把 `chi_2n^2` 排印为自由度不清的形式；正文统一为 `chi^2_{2n}`。95% 数值端点仍按原稿 9.59 与 34.17 保留。
5. Poisson--Gamma 可信区间的卡方分位数下标在原稿中上下尾顺序混乱；正文改用后验分布的 `alpha/2` 与 `1-alpha/2` 分位数定义，同时保留原稿数值区间。
6. 正态 Bayes 区间与频率覆盖概率的两个推导均已保留，并明确可信概率与覆盖概率来自不同的随机机制。
7. 最短 Gamma 枢轴区间的目标是 `1/a-1/b` 而不是 `b-a`；正文保留原稿的约束优化观点并修正积分方向。
8. `HPD_case.R` 依赖当前环境未安装的 `coda::HPDinterval()`。迁移后保留观测值、Gamma 先验、随机种子、100000 次后验抽样与最短样本窗口的 HPD 思路，仅以基础 R 的排序窗口计算替代 `coda` 函数。
9. `501011.pdf`、`501012.pdf` 转为网页 PNG；`HPD.png` 原样复制。原始文件均未修改。
10. 本轮逐项核对 Casella--Berger 第 9.1--9.3 节（印刷页 417--444；PDF 页 445--472）与 `legacy/latex/课程课件B/tex/chap-9/`。定理 9.2.2 的检验--置信集双向证明、例 9.2.1 的正态检验反演、例 9.2.3 的指数 LRT 反演、定理 9.3.2 的最短区间证明和例 9.3.4 的 Gamma 尺度变换均整理为中英文折叠内容。
11. 定理 9.3.2 的证明保留教材的单峰性、等端点密度和区间包含众数三个条件，并按候选区间相对位置分情形比较概率质量。例 9.3.4 明确优化目标是变换回参数空间后的长度 `x(1/a-1/b)`，没有把枢轴尺度上的等密度条件误作最终最优条件。
12. 教材第 9.4 节习题没有随项目提供答案；本轮未补造标准答案。已有 Poisson--Gamma 与正态 Bayes 数值继续按原课件保留。

## 第 10 章

1. 原稿把渐近方差定义中的归一化笼统写作 `k_n(T_n-tau(theta))`，同时效率部分使用 `sqrt(n)`；正文以一般 `a_n` 定义渐近方差，并在常规 iid 效率结论中明确使用 `sqrt(n)`。
2. 渐近相对效率定义第二行漏写反斜线，形成 `sqrt{n}` 文本；正文修复为标准数学公式，并解释当前比值方向。
3. MLE 正则条件原稿多处拼写错误，且引用未随课件迁移的 Miscellanea 10.6.2；正文完整列出课件给出的六项条件，并说明它们是充分条件而非唯一条件集。
4. Bootstrap 全枚举方差与 B 次近似均保留；正文明确区分 `B -> infinity` 的 Monte Carlo 收敛和 `n -> infinity` 的 Bootstrap 一致性。新增基础 R 重抽样图，不替换原方法。
5. Huber 损失原稿两段条件在 `|x|=k` 处重叠但数值一致；正文用 `<=` 与 `>` 明确分段，并新增损失曲线。
6. 复合原假设 LRT 渐近自由度原稿把“原假设自由参数数减去全空间自由参数数”写反；正文更正为无约束维数减受限维数 `p-q`。
7. 原稿使用 `chi^2_{nu,alpha}` 但未明确上下尾分位数约定；正文统一使用累积分位数 `chi^2_{nu,1-alpha}`。
8. 近似 ML 区间的方差式在正文中把导数与观测信息都明确在 MLE 处评价。二项 LRT 区间保留原似然比形式，并说明端点采用一维求根。
9. 本轮逐项核对 Casella--Berger 第 10.1、10.3、10.4 节（印刷页 465--504；PDF 页 493--532）与 `legacy/latex/课程课件B/tex/chap-10/`。例 10.1.2 的相合性计算、定理 10.1.12 的得分 Taylor 展开、定理 10.3.1 的二阶似然展开及例 10.4.3 的二项 LRT 区间均加入中英文折叠推导。
10. 教材对定理 10.1.6 只给强大数定律与期望对数似然的证明纲要，并把完整技术证明指向外部文献；正文因此统一使用“查看推导过程 / View derivation”，明确保留一致收敛、紧性等未展开条件，没有将纲要冒充完整证明。
11. 教材定理 10.1.12 只详细说明 `hat(theta)` 的渐近正态性，并把推广到 `tau(hat(theta))` 留作 Exercise 10.7；正文恢复前一部分，并仅用已学 Delta 方法说明推广。二项 LRT 区间的边界样本说明来自似然的参数空间边界，不改变原区间定义。

## 第 7--9 章课堂教学扩展（2026-09-02）

以下内容是教师明确允许“不局限于教材和 tex 文件”后增加的教学实验，不作为 Casella--Berger 原文或原课件内容呈现。中英文页面保持相同统计设定、随机种子和结论。

1. 第 7 章增加 Bernoulli 模型估计 $p^2$ 的 Rao--Blackwell 模拟。公式由条件组合概率直接推出，程序以固定随机种子比较 $X_1X_2$ 与 $T(T-1)/\{n(n-1)\}$ 的重复抽样方差。
2. 第 8 章增加已知方差正态均值双侧 $z$ 检验的功效曲面，以及原假设和固定备择下的 p 值抽样分布。两项均使用解析正态分布生成，不引入外部数据或新依赖。
3. 第 9 章增加正态均值区间的重复抽样覆盖演示，并比较 Wald、Wilson、似然比反演与 Clopper--Pearson 二项比例区间。覆盖率通过枚举全部二项结果精确求和，不使用 Monte Carlo 近似。
4. 第 7--9 章新增统一的“产品缺陷率”教学案例：$n=200,y=16,p_0=0.05$。第 7 章比较 MLE 与 Beta(2,38) 先验下的后验均值，第 8 章使用精确单侧二项检验，第 9 章比较四种双侧区间并解释单侧检验与双侧区间的对偶层级。
5. 新增 `R/inference_labs.R`，集中保存二项似然比偏差、四类区间和精确覆盖率函数；仅使用基础 R，避免新增构建依赖。

## 第 1--2 章证明与解答补全（2026-09-01）

本轮只修改第 1、2 章中英文讲义及折叠样式。新增证明与解答均以 `Statistical_Inference_Casella_Berger.pdf`、原 LaTeX 课件和既有 Rmd 为依据；学生页面不显示以下溯源说明。

### 第 1 章溯源

1. 离散样本空间概率定理：教材 Theorem 1.2.6（印刷页 8--9；PDF 页 35--36），对应 `legacy/latex/课程课件B/tex/chap-1/1-2_PF.tex`。
2. 条件概率满足 Kolmogorov 公理：原课件 `1-3_CP.tex` 的 “Problem 1.41”；教材在 §1.3 将此任务指向 Exercise 1.35。教材实际 Exercise 1.41 是电报码点划问题，因此正文明确“课件编号”，避免误作教材题号。
3. 连续抽到四张 A：教材 Example 1.3.3（印刷页 21--22；PDF 页 48--49）及 `1-3_CP.tex`。原课件第一个小数 `0.0005` 与组合公式不一致；正文保留已核算的约 `0.000048`。
4. 三名死囚：教材 Example 1.3.4（印刷页 22--23；PDF 页 49--50）提供完整题干、狱卒回答规则和 $1/3$ 解答；原课件 `1-3_CP.tex` 只保留开头。本轮据教材恢复完整版本，并撤销旧迁移说明中“题干不足”的判断。
5. Bayes 公式：教材 Theorem 1.3.5（印刷页 23；PDF 页 50--51）及 `1-3_CP.tex`；证明按划分、全概率公式与乘法公式展开。
6. 编码信息传输：教材 Example 1.3.6（印刷页 23--24；PDF 页 51）及 `1-3_CP.tex`，保留点号/划号先验、$1/8$ 误传率和 $21/25$ 结果。
7. 四次掷骰至少一个 6、生日问题、补事件独立性：原课件 `1-3_CP.tex`；补事件定理另对应教材 Theorem 1.3.9（印刷页 25；PDF 页 52--53）。生日问题在现有课件中只有标题，本轮仍标记“资料不足，未补充”。
8. 两骰子独立性反例：教材 Example 1.3.10（印刷页 26；PDF 页 53--54）及 `1-3_CP.tex`，补回 36 个有序结果、各点数和计数及两两交集计算。
9. cdf 刻画：教材 Theorem 1.5.3（印刷页 31；PDF 页 58--59）只给必要性证明提纲，并明确说明充分性证明更困难且省略。本轮仅恢复该提纲，不自行补造测度扩张证明。

### 第 2 章溯源

1. 正弦平方、倒数 Gamma、标准正态平方：教材 Examples 2.1.2、2.1.6、2.1.9（印刷页 49--54；PDF 页 77--82）及 `legacy/latex/课程课件B/tex/chap-2/2-1_DF.tex`。标准正态平方在课件中标作 Example 2.1.7，正文保持课程编号并继续使用已更正的密度符号 $\phi$。
2. 单调变换、分段单调变换、概率积分变换：教材 Theorems 2.1.5、2.1.8、2.1.10（印刷页 51--55；PDF 页 79--83）及 `2-1_DF.tex`。2.1.8 教材只解释分支求和思想，折叠区标为“查看推导过程”，不冒充完整证明。
3. 指数均值、二项均值、Cauchy 均值：教材 Examples 2.2.2--2.2.4（印刷页 55--57；PDF 页 83--85）及 `legacy/latex/课程课件B/tex/chap-2/2-2_EV.tex`，补回分部积分、组合恒等式和绝对可积性检查。
4. 期望性质与平方损失中心：教材 Theorem 2.2.5、Example 2.2.6（印刷页 57--58；PDF 页 85--86）及 `2-2_EV.tex`，补回连续情形证明和平方展开。
5. MGF 生成矩：教材 Theorem 2.3.7（印刷页 62--63；PDF 页 90--91）及 `legacy/latex/课程课件B/tex/chap-2/2-3_MM.tex`，并把交换求导与积分的条件指回 §2.4。
6. 相同矩的不同分布：教材 Example 2.3.10（印刷页 64--65；PDF 页 92--93）及 `2-3_MM.tex`，补回对数替换、配方和奇偶性论证。
7. MGF 唯一性与收敛：教材 Theorems 2.3.11--2.3.12（印刷页 65--66；PDF 页 93--94）明确省略依赖 Laplace 变换的技术证明；本轮标记“资料不足，未补充”，没有用模型推导替代。
8. Poisson 近似：教材 Example 2.3.13 与 Lemma 2.3.14（印刷页 66--67；PDF 页 94--95）及 `2-3_MM.tex`，补回 MGF 极限链；继续保留“近似/极限”而非课件的概率相等表述。
9. Leibniz 法则、支配收敛、积分号下求导：教材 Theorems 2.4.1--2.4.3（印刷页 68--71；PDF 页 96--99）及 `legacy/latex/课程课件B/tex/chap-2/2-4_DU.tex`。教材说明一般详细证明不在本书展开；仅恢复微积分基本定理、差商与支配收敛之间的推导链。
10. 正态 MGF：教材 Example 2.4.6（印刷页 72--73；PDF 页 100--101）及 `2-4_DU.tex`，补回配方和正态密度归一化步骤。
11. 级数交换定理 2.4.8、2.4.10：教材印刷页 74--76（PDF 页 102--104）只给定理陈述及相邻示例，没有一般证明；本轮标记“资料不足，未补充”，未生成无来源证明。
